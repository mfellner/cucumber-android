Run `ant dependencies` to download the dependencies.

The following jars will be downloaded:
cucumber-core-1.1.3.jar
cucumber-java-1.1.3.jar
cucumber-junit-1.1.3.jar
hamcrest-core-1.3.jar
junit-4.11.jar

When building with ant, make sure to set `$ANDROID_HOME or create local.properties with `sdk.dir=/path/to/sdk`.
